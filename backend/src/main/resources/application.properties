# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hrms?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Admin@123

  

  

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true 

# Application Configuration
spring.application.name=HRMS Backend

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB

# File Storage Path
file.upload-dir=./uploads
file.document-dir=./uploads/documents
file.payslip-dir=./uploads/payslips
file.offer-letter-dir=./uploads/offer-letters

# Email Configuration (Update with your SMTP settings)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=noreply@primesourcellp.com
spring.mail.password=hcig oift fotd kzdx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration
# Secret key: 256-bit (32 bytes) Base64-encoded random key for HMAC-SHA256 signing
# Generated using: RNGCryptoServiceProvider (cryptographically secure)
jwt.secret=8i470klppPQCmEcraOhFcBt5Bvs6x5btoFN9O7b4xok=
jwt.expiration=86400000
jwt.refresh-expiration=604800000